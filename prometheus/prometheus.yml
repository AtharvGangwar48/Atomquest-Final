global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'supportvision'
    static_configs:
      - targets: ['host.docker.internal:3001']
    metrics_path: '/metrics/prometheus'
